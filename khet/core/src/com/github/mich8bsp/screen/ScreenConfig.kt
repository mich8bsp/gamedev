package com.github.mich8bsp.screen

object ScreenConfig{
    val viewportWidth = 850f
    val viewportHeight = 780f

    val screenWidth = viewportWidth.toInt() * 2
    val screenHeight = viewportHeight.toInt() * 2
}